<div class="card card-block">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-block btn-success" formnovalidate>Save</button>
    {{ form_end(form) }}
</div>
